[package]
name = "solver"
version = "0.2.7"
authors = ["Wataru Inariba"]
edition = "2021"
license = "AGPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]
path = "../solver-src/lib.rs"

[dependencies]
js-sys = "0.3.95"
postflop-solver = { path = "../../../postflop-solver-upstream", features = ["custom-alloc", "rayon"], default-features = false }
rayon = { version = "1.8.0", optional = true }
spmc = "0.3.0"
wasm-bindgen = "0.2.118"

[features]
default = ["rayon"]
wgpu-backend = ["postflop-solver/wgpu-backend"]

[profile.release]
codegen-units = 1
lto = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = ["-O4"]
